# Mac Client

macOS application implementation home. This application will be built and tested
on the actual Mac later. No macOS implementation is included yet.

The app discovers Windows services, coordinates a session, receives UDP audio,
and plays through Core Audio according to the [protocol design](../docs/README.md).
